#include <stdio.h>

int main()
{

    int a = 50, b = 222;
    // this is called ternary operator it just simplify if else statement
    a > b ? printf("a is greater than b") : printf("b is grater than a");

    return 0;
}