#include <stdio.h>

int main()
{
    int a;
    char ch = 'a';
    ch = 'c';
    ch = '\n';

    //  we can use multiple "" like the given example

    printf("Yea I can access \" the Dynamic variable on the Print\" ");

    return 0;
}